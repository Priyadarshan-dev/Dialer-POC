import UIKit

// Minimal SceneDelegate to satisfy Xcode's build system.
// Note: Scene support is disabled in Info.plist to prevent Flutter engine conflicts.
class SceneDelegate: UIResponder, UIWindowSceneDelegate {
    var window: UIWindow?
}
